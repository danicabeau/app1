import React from 'react' // เติม React
import FormText from './form-text' // เติม ./form-text (ชื่อไฟล์คอมโพเนนต์)
import FormSelectFile from './form-select-file' // เติม ./form-select-file (ชื่อไฟล์คอมโพเนนต์)

export default function App() {
    return <FormSelectFile/>
}